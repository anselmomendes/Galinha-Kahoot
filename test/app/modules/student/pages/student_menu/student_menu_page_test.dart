import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:galinha_karoot/app/modules/student/pages/student_menu/student_menu_page.dart';

main() {
  testWidgets('StudentMenuPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StudentMenuPage(title: 'StudentMenu')));
    //  final titleFinder = find.text('StudentMenu');
    //  expect(titleFinder, findsOneWidget);
  });
}
