import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:galinha_karoot/app/modules/cases/pages/navigator_bar/cases_quiz/cases_quiz_page.dart';

main() {
  testWidgets('CasesQuizPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CasesQuizPage(title: 'CasesQuiz')));
    //  final titleFinder = find.text('CasesQuiz');
    //  expect(titleFinder, findsOneWidget);
  });
}
